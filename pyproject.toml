[project]
name = "transcript-toolkit"
version = "0.1.0"
description = "Generic meeting transcript correction system"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest >=8.0"]
